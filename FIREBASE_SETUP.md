# ربط BOOST STORE بـ Firebase

## 1) رابط قاعدة البيانات
افتح Firebase Console ← Realtime Database، وانسخ الرابط الظاهر أعلى شجرة البيانات،
ثم ضعه في `SaadApp/Services/AppConfig.cs` في `DatabaseUrl`.

## 2) قواعد الحماية (مهم جداً)
Realtime Database ← Rules ← الصق محتوى الملف `database.rules.json` ← Publish.

هذه القواعد تسمح للتطبيق فقط بـ:
- قراءة عقدة `update`.
- قراءة كود واحد يعرفه المستخدم (`users/الكود`) — لا يمكن عرض قائمة كل الأكواد.
- تفعيل كود جديد **مرة واحدة فقط** (عندما يكون `device_id` فارغاً والكود مفعّل)،
  ولا يمكن تغيير `subscription_type` أو `subscription_active` أو ربط الكود بجهاز آخر.

## 3) شكل البيانات
```
update/
  Update URL:    "https://..."
  Update number: 20.2
users/
  <الكود>/
    device_id:           ""        ← يُملأ تلقائياً عند أول تفعيل
    subscription_active: true      ← false = إيقاف الكود وطرد المستخدم فوراً
    subscription_date:   ""        ← يُملأ تلقائياً بالميلادي (توقيت UTC، أي السعودية - 3 ساعات)
    subscription_type:   "30"      ← lifetime | 1day | أي رقم = أيام
    user_name:           ""        ← اسم مستخدم ويندوز، يُملأ تلقائياً
    activated_at:        (رقم)     ← وقت الخادم لحظة التفعيل، يُضاف تلقائياً
```

- **إنشاء كود:** أضف عقدة تحت `users` باسم الكود، والحقول كما في الأعلى مع `device_id` فارغ.
  الكود يقبل **أحرف عربية أو إنجليزية** وأرقام و `-` و `_` ومسافة في الوسط (مثل `كود-سعد-٢٠٢٥`).
  ممنوع في Firebase: `.` `$` `#` `[` `]` `/`. الأحرف الإنجليزية الكبيرة والصغيرة مختلفة (`ABC` ≠ `abc`).
  استخدم أكواداً طويلة وعشوائية (مثل `BST-7K2Q-9XMA-4PLD`) — الأكواد القصيرة مثل `305` يمكن تخمينها.
- **نقل الكود لجهاز جديد:** امسح `device_id` و `activated_at` (المدة تبدأ من جديد عند التفعيل التالي).
- **تمديد الاشتراك:** غيّر `subscription_type` (يُطبَّق فوراً على التطبيق المفتوح).
- **نشر تحديث:** زِد `CurrentVersion` في `AppConfig.cs`، ابنِ النسخة وارفعها، ثم حدّث `Update number` و `Update URL`.

## ⚠️ المفتاح الخاص (service account)
التطبيق **لا يحتاج** أي مفتاح خاص، ولا يجب وضعه داخل التطبيق أبداً
(أي شخص يملك ملف exe يستطيع استخراجه والتحكم الكامل بقاعدة البيانات).
